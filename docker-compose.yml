services:
  sos-guide:
    build:
      context: docker/
    ports:
      - "8080:80"
    volumes:
      - ./src:/app/src:ro
    environment:
      - DEV_MODE=1

# Accès :
#   Starter (wizard config) : http://localhost:8080/
#   Portail survivants       : http://localhost:8080/portal
#   Admin                    : http://localhost:8080/admin
#                              (login: admin / password: dev-sos-guide)
